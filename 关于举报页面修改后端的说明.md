# 1.管理员举报页面的提交修改内容

原有每条举报数据里多加了一个 `target`字段，原本所有返回字段、数据格式全部保留，原有依赖旧接口的业务不会受任何影响。

主要修改的后端代码：
report.py

admin_service.py

report_service.py


# 2.个人举报列表页面的修改内容


核心是为 `GET /api/reports`（"我的举报"列表）的每条记录多附带一个 `target` 字段，用于返回被举报对象（商品/用户/订单）的摘要详情。 **原本接口返回的所有字段、整体数据格式全部保持不变** ，只是在原有数据基础上新增了一个可选字段，原有依赖该接口的业务不会受任何影响。

主要修改的后端代码：

* **`app/api/routers/reports.py`** — 为 `list_my_reports` 路由注入数据库会话 `db: Session`（与管理端路由保持一致），以便调用带 target 详情的查询。
* **`app/services/report_service.py`** — `list_my_reports` 内部改用 `list_reports_with_target`，复用管理端已有的对象摘要查询逻辑，为每条举报附带 `target` 详情。

兼容性说明：

* 返回的 `list` 结构、分页字段 `page`、筛选字段 `filters` 均未改动，只是 `list` 内每项多了 `target` 字段。
* `admin_service.py`、举报处理（handle）、创建举报（create）、申诉等链路 **完全未触碰** ，管理后台行为不受影响。
* 改动仅限"我的举报"这一个读取接口的返回内容增强，属于向后兼容的增量改动。
