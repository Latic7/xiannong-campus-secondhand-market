<image class="page-bg" src="/assets/images/Background.jpg" mode="aspectFill" />
<view class="page">
  <!-- 管理功能菜单 -->
  <view class="admin-menu">
    <view class="menu-item" bindtap="onReviewProducts">
      <view class="menu-left">
        <image class="menu-icon-img" src="/assets/icons/package.svg" mode="aspectFit" />
        <text class="menu-text">商品审核</text>
      </view>
      <view wx:if="{{pendingProducts > 0}}" class="badge">{{pendingProducts > 99 ? '99+' : pendingProducts}}</view>
      <text class="menu-arrow">›</text>
    </view>

    <view class="menu-divider"></view>

    <view class="menu-item" bindtap="onReviewReports">
      <view class="menu-left">
        <image class="menu-icon-img" src="/assets/icons/warning.svg" mode="aspectFit" />
        <text class="menu-text">举报审核</text>
      </view>
      <view wx:if="{{pendingReports > 0}}" class="badge badge-warning">{{pendingReports > 99 ? '99+' : pendingReports}}</view>
      <text class="menu-arrow">›</text>
    </view>
  </view>
</view>
