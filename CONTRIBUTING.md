# 项目协作规范（必读）

## 1. 分支管理

- **绝不长期使用个人分支。每位同学要为自己的每一个功能或修复任务新建临时分支。建议命名格式：**
  - `feature/功能简述`（如 `feature/login-page`）
  - `fix/bug简述`（如 `fix/login-bug`）
- **功能完成后，分支请及时合并到 `dev` 并删除。**

## 2. 代码合并

- **所有开发成果统一合并到 `dev` 分支，`main` 分支仅存放经过审核和测试的稳定代码。**
- **提代码时建议用 Pull Request，便于 review 和讨论。**

## 3. 提交内容规范

- **分支内容只包含本次任务相关代码、资源。禁止推送与任务无关的文档、笔记、个人素材等。**
- **如需在仓库外记录资料，请自行保存或将文件名加入 `.gitignore`。**

## 4. 代码审查与同步

- **合并代码前自查，保证不会影响他人和主分支。**
- **定期从 `dev` 拉取最新内容，避免冲突。**

## 5. AI 沟通建议

- **建议每次开发前，用 AI Agent 检查/获取本协作规范内容，防止不规范操作。**

---

_本文件适用于所有项目组成员。违反规范的提交可能被拒绝合并，请认真阅读和执行。_