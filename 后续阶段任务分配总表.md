# 后续阶段任务分配总表（第二阶段到收尾）

本表用于一次性布置后续全部开发任务。

适用前提：第一阶段主链路已可联调（登录、浏览、发布、下单、举报、后台最小能力）。

---

## 0. 一些重要的提示

### Tip 1：要开发完整的链路，而不是真的各做各的。

在后续开发的时候，尽可能完成完整的链路。所以每当你开发完一个模块，就检查一下当前项目里有没有别的模块**本应调用你的模块但是还在使用mock数据**。如果有的话，就把这些mock数据给移除了，然后把你的模块真正接进去。

### Tip 2：如何接入并同步数据库？

**后端D**需要安装一下HeidiSQL作为自己的SQL客户端。在自己本地，后端D需要做如下的事情：

1）搭建完整的数据库，可以塞一些数据进去用于测试。你的MySQL服务器必须位于3306端口上。

2）新建一个用户！去新建一个用户，比如说名为“campus_market_database_user”，密码可以设置一个简单的“123456”，然后赋予它对后端数据库的完整读写权限。我已经把这些信息存放到.env.example里了。

3）让后端真正接入MySQL数据库。现在的后端还没有真的接进MySQL数据库，一直在回退成SQLite。按上面的信息去接入一下。

4）验证成功接入以后，在HeidiSQL里右键campus_market，选择导出为SQL脚本，命名为“create_campus_market_database.sql”，勾选“包含数据”和“包含删除语句”，然后把这个SQL脚本放到Git仓库目录的最外层（就像现在这样）。

5）每次你对数据库有什么更新，就像（4）里这样新建一个用于建库的SQL脚本，替换掉Git仓库里那个旧的，然后在微信群里通知所有人。

后端D做好这些工作以后，**其他人**需要做的是：

1）安装自己喜欢的SQL客户端（可以用HeidiSQL，也可以不用，但是必须有一个能用的SQL客户端），并在自己电脑的3306端口上开一个MySQL 8.0服务器。

2）为了获取数据库，你需要运行后端D提供的这个SQL脚本。

3）每次后端D更新他的建库脚本，所有人都要在本地运行一下它来获取最新的数据库。

4）为了接入数据库，你还需要自己创建一个一模一样的用户以及一模一样的密码（名字为“campus_market_database_user”，密码为“123456”），并且赋予它对“campus_market”的全部读写权限。

### 常见问题：

#### 1）我为什么要安装MySQL服务器？听说很麻烦

MySQL 8.0的安装可以直接使用MySQL Installer进行安装。在整个过程中，你除了设置一下root用户的密码以及把密码格式改为5.7兼容模式以外，其余的只需要你一路点`确认`就行了。整个安装过程不会超过3分钟。这都嫌麻烦那我真的没话说了。

#### 2）我一定要把MySQL服务器挂在3306吗？我的3306上已经有别的东西了

不一定要挂在3306上，去找你自己的.env然后把端口号改成你自己的端口号就行了。

#### 3）我非得要用什么“campus_market_database_user”吗？

不一定，同理（2），你去.env改一下就行了。

#### 4）创建用户好麻烦，我不创，我要用root

随你便。

#### 5）我一定要安装一个MySQL客户端吗？

你要是知道用别的方法操作你本地的MySQL数据库的话那我不管，但是如果你单纯是不愿意操作你本地的MySQL数据库的话那我给你踹了。如果你担心客户端占用太多空间、安装麻烦、卸载不干净：那就装HeidiSQL啊！

#### 6）为啥我每次都要运行后端D新发的脚本？运行这个脚本是不是很麻烦，会不会搞得我硬盘爆炸？

不运行最新的脚本，你就没法同步最新的数据库结构，不同步最新的数据库结构，你后端做的就会和别人不一样，后端做得和别人不一样，你的项目跑起来就会出错，跑起来就会出错，你要不就是找我问为什么要不就是啥也不说直接推了个PR，然后项目就炸了，别人也炸了。

除非你还在用上个世纪的电脑，不然电脑不会炸。运行脚本不会在你的硬盘里产生奇怪的东西，硬盘不会炸（因为脚本会把旧的库删了，不会给你留一堆旧数据）。运行脚本就是进你的客户端然后鼠标点一下，如果这都嫌麻烦的话那我真的没话说了。

#### 7）豆包告诉我我的数据库做的不够好，所以我让它改了一下并且按这个新的继续做

枪毙。

---

## 1. 总目标（后续阶段）

后续阶段统一目标是：

1. 把第一阶段“能跑通”提升为“可稳定使用”。
2. 把状态机、权限边界、异常返回、分页查询彻底对齐 OpenAPI。
3. 把测试、数据库迁移、统计、后台治理能力补齐到可验收水平。
4. 形成可演示、可答辩、可继续迭代的版本。

---

## 2. 阶段划分与里程碑

建议按 3 个阶段推进，假如已经完成第一阶段的话：

### 2.1 第二阶段：功能完善与规则落地（建议 7-10 天）

阶段目标：

- 补全第一阶段未做完接口（尤其管理员用户管理、订单异常路径、商品审核细节）。
- 完成关键状态机约束和权限校验。
- 完成前端主页面业务动作闭环（收藏、举报、下单、评价、订单流转反馈）。

里程碑：

1. M2-1：所有主业务接口字段与状态值 100% 对齐 OpenAPI。
2. M2-2：前端 6 个核心页面都能完整走主路径 + 1 条失败路径。
3. M2-3：联调问题单收敛到“无阻塞级问题”。

### 2.2 第三阶段：治理、性能与稳定性（建议 5-7 天）

阶段目标：

- 强化后台治理能力（举报处理、日志追踪、统计可解释）。
- 完成数据库索引优化、迁移脚本完善、基础压测和慢查询修复。
- 提升错误处理质量、幂等性、并发场景稳定性。

里程碑：

1. M3-1：交易、举报、审核链路具备可审计日志。
2. M3-2：关键列表接口分页性能达标（常规数据量下响应稳定）。
3. M3-3：回归测试通过率达到可发布阈值。

---

## 3. 固定角色分工（后续阶段沿用）

1. 前端 A：主页、列表、通用组件体验与列表性能。
2. 前端 B：详情、发布、个人中心、业务动作闭环。
3. 后端 A：认证、用户、权限与统一响应/错误处理。
4. 后端 B：商品、订单、状态机与交易幂等。
5. 后端 C：举报、后台治理、统计与管理员日志。
6. 后端 D（测试）：契约测试、联调回归、数据库建模与迁移协同。

说明：如人力紧张，后端 D 在第二阶段中后期优先测试，不再承担新增开发任务。

---

## 4. 第二阶段任务分配（功能完善与规则落地）

### 4.1 前端 A

必做任务：

1. 首页、列表页补齐筛选排序、分页、下拉刷新、错误态。
2. 统一 item-card 展示逻辑（状态标签、价格格式、空图占位）。
3. 完成列表类页面的“加载中/空状态/错误重试”统一交互。
4. 对接分类入口与关键词检索，保证参数名与后端一致。

阶段交付物：

- 首页与列表页可稳定联调。
- item-card 组件支持多状态展示。
- 列表相关联调问题清单（前端视角）。

### 4.2 前端 B

必做任务：

1. 详情页接通收藏、举报、下单、状态反馈。
2. 发布页补齐字段校验、图片上传、提交态与失败回滚提示。
3. 个人中心补齐“我的发布/我的订单/我的收藏/我的举报”。
4. 统一登录过期处理（跳转、提示、重试策略）。

阶段交付物：

- 详情页具备完整业务动作闭环。
- 发布页可完成一次真实发布流程。
- 用户中心可串起个人业务数据。

### 4.3 后端 A

必做任务：

1. 完成 auth、users 接口异常码统一与响应格式收敛。
2. 完成管理员用户管理接口：用户分页查询、封禁/解封。
3. 强化登录态校验、token 刷新边界和未登录访问拦截。
4. 输出权限校验清单（普通用户、卖家、管理员）。

阶段交付物：

- 认证与用户模块通过契约检查。
- 管理员用户管理接口可联调。
- 权限边界说明文档（简版即可）。

### 4.4 后端 B

必做任务：

1. 商品发布、更新、下架、审核前后状态约束落地。
2. 订单状态机严格校验：创建、确认、取消、完成、评价。
3. 增加关键接口幂等与冲突处理（重复下单、重复确认等）。
4. 图片上传接口补齐最小可用能力（URL 返回与基础校验）。

阶段交付物：

- 商品与订单接口状态流转可验证。
- 交易失败路径返回码清晰可用。
- 幂等冲突测试记录。

### 4.5 后端 C

必做任务：

1. 举报提交、详情、后台处理链路补齐处理动作字段。
2. 后台举报队列支持基础筛选和状态过滤。
3. 统计接口补齐总览、商品、交易、用户维度最小可用数据。
4. 管理员日志结构与查询接口可用。

阶段交付物：

- 举报治理链路可联调。
- 后台统计接口可被前端展示。
- 管理日志可追踪关键操作。

### 4.6 后端 D（测试+数据库协同）

必做任务：

1. 更新契约测试清单，覆盖第二阶段新增接口与状态分支。
2. 建立主链路回归清单（成功+失败路径）。
3. 协同完善数据库迁移脚本与初始化脚本。
4. 联调期按天维护问题台账并标注优先级。

阶段交付物：

- 契约测试报告（字段、状态、结构一致性）。
- 回归测试清单 v2。
- 迁移脚本执行记录。

---

## 5. 第三阶段任务分配（治理、性能、稳定性）

### 5.1 前端 A

1. 列表与首页性能优化（避免重复请求、分页去重、缓存策略）。
2. 统一全局错误提示与重试交互。
3. 关键页面埋点（如列表加载耗时、失败率）。

### 5.2 前端 B

1. 发布、详情、个人中心的异常路径体验优化。
2. 订单与举报状态展示文案统一，避免“状态值和文案不一致”。
3. 演示用关键流程 UI 打磨（保证讲解时可读性）。

### 5.3 后端 A

1. 统一错误码字典与异常映射，补齐文档。
2. 优化认证与用户接口日志，支持 requestId 全链路追踪。
3. 补充权限越权测试样例。

### 5.4 后端 B

1. 订单并发场景加固（库存/可售状态竞争、重复提交）。
2. 商品与订单常用查询索引复核并优化。
3. 完成交易链路的稳定性回归修复。

### 5.5 后端 C

1. 举报处理流程补充审计信息（操作人、动作、原因、时间）。
2. 统计接口可解释性优化（字段定义清晰、口径一致）。
3. 管理后台核心查询接口性能优化。

### 5.6 后端 D（测试）

1. 执行全链路回归（登录、发布、下单、举报、后台）。
2. 增加性能基线测试（关键列表、详情、订单操作）。
3. 输出缺陷分布分析（字段、状态、权限、性能四类）。

第三阶段验收标准：

1. 关键接口无阻塞级缺陷。
2. 状态机违规路径均被拦截且返回可解释错误。
3. 管理日志与统计数据可用于答辩演示。

---

## 6. 第四阶段任务分配（验收、演示、答辩收尾）

### 6.1 全员共同任务

1. 只修复高优先级问题，不新增业务需求。
2. 冻结接口字段与状态枚举，不做破坏性调整。
3. 每日同步“待修复问题 + 风险 + 次日计划”。

### 6.2 分角色收尾重点

1. 前端 A：首页/列表演示脚本与兜底页准备。
2. 前端 B：详情/发布/个人中心演示脚本与异常场景演示准备。
3. 后端 A：认证、用户、权限说明与统一错误码说明。
4. 后端 B：商品/订单状态机讲解材料与关键日志样例。
5. 后端 C：举报/后台/统计讲解材料与治理闭环证据。
6. 后端 D：测试报告、问题收敛曲线、回归结论。

第四阶段交付物：

- 演示脚本（主流程 + 异常流程）。
- 最终测试报告。
- 最终接口对齐报告。
- 部署说明与回滚预案。

---

## 7. 团队协作机制（建议直接执行）

1. 每晚 10-15 分钟站会：只讲阻塞项、风险、次日目标。
2. 每个任务必须绑定“责任人 + 截止时间 + 验收人”。
3. 联调问题统一进一个台账，按 P0/P1/P2 标级并每日清零高优先级。
4. 所有字段改动先改 OpenAPI，再改后端，再改前端。
5. 提交前必须自测至少 1 条成功路径 + 1 条失败路径。

---

## 8. 统一 DDL 建议（后续总计划）

建议采用“第二阶段 8 天 + 第三阶段 6 天 + 第四阶段 4 天”的节奏，共 18 天：

1. 第 1-8 天：完成第二阶段功能完善与规则落地。
2. 第 9-14 天：完成第三阶段治理、性能与稳定性。
3. 第 15-18 天：完成第四阶段验收、演示与答辩收尾。

关键控制线：

1. 第 8 天晚：必须有可联调稳定版本。
2. 第 14 天晚：必须完成高优先级缺陷清理。
3. 第 18 天：必须完成可演示、可验收、可提交版本。

---

## 9. 给组长的执行建议

1. 不再按“阶段开始时再分工”，改为本表一次性布置，阶段内只调优先级。
2. 每个成员固定主责域，减少跨域切换成本。
3. 你只盯三件事：状态机一致性、字段一致性、高优先级缺陷清零。
4. 每阶段结束时只做复盘，不重新洗牌分工。

这样可以显著降低沟通成本，并保持推进节奏稳定。